DirectoryIndex index.php index.html
ErrorDocument 404 /index.php

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si la ruta empieza con /admin, dejar que admin/.htaccess maneje
    RewriteRule ^admin(/|$) - [L]

    # Servir assets del build de web desde /dist/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^assets/(.*)$ dist/assets/$1 [L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^favicon\.svg$ dist/favicon.svg [L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^icons\.svg$ dist/icons.svg [L]

    # Servir archivos existentes directamente
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule . - [L]

    # Todo lo demás → web SPA
    RewriteRule ^(.*)$ index.php [L]
</IfModule>
